import Link from "next/link";
const letter_sample = "./Letters/0001-00-01_be_loved_universe.html"; 
import parse from 'html-react-parser'

const SiteLetters = () => {
    return ( 
        <div>
           ✔️ Successfully Loaded The Letters Project.  <br /> 
            <Link href=" ">
            <a> 📖Letter 1</a>
            </Link>
        </div>
     );
}
 
export default SiteLetters;

{/* It is highly unlikely that loading letters will work using next's methods. 
Instead I should somehow load in pages as an iFrame 
*/}